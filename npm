#!/usr/bin/env sh
# Local shim for environments where npm is unavailable in PATH during plugin install.
# This plugin is self-contained at runtime and does not require dependency installation.
echo "[qqbot] npm shim: skip $*"
exit 0
